# 按键映射助手 KeyMapper v2.3

一个简单易用的 Windows 键盘改键工具，支持按键测试、开机自启、系统托盘后台运行。

## 功能

- **按键映射**：把任意按键改成其他按键或功能，共四种动作类型：
  1. **映射为按键 / 功能**：支持 136 种目标：
     - 媒体控制：音量增大/减小/静音、播放/暂停、上一曲/下一曲、停止
     - 浏览器：后退、前进、刷新、停止加载、搜索、收藏夹、主页
     - 应用键：电子邮件、我的电脑、计算器、媒体播放器
     - 功能键：F1 - F24
     - 系统键：Esc、PrtSc、ScrollLock、Pause、Insert/Delete/Home/End、翻页、方向键、退格、回车、Tab、CapsLock、空格、NumLock、Win、菜单键、Shift/Ctrl/Alt（左右可分别映射）
     - 小键盘：0-9、小数点、+、-、*、/、回车
     - 字母键 A-Z、数字键 0-9、全部符号键
  2. **打开网页**：按下该键用默认浏览器打开指定网址（缺少协议时自动补全 https://）
  3. **启动程序**：按下该键启动指定程序（exe、快捷方式 .lnk、批处理均可，支持"浏览…"选择）
  4. **屏蔽此键**：按下无任何反应（适合屏蔽误触键）
  （网页/程序动作只在按下时触发一次，长按不会重复启动）
- **编辑映射**：选中列表中的映射后点【编辑选中】，或直接**双击列表行**，可修改已设置的映射（来源键和动作都可改，已预填原值）
- **删除/清空**：删除选中映射、一键清空全部
- **按键测试**：打开测试窗口后，按下键盘任意键，屏幕上的模拟键盘对应按键会高亮显示，同时显示键名和虚拟键码
- **注入自检**：一键检测映射是否被系统接收（模拟按 F13 和音量+，检查注入结果和音量变化），用于排查问题
- **任务栏滚轮调音量（v2.3 新增）**：主界面右下角的开关一键启用
  - 把鼠标移到底部任务栏上，**滚动滚轮**即可调节系统主音量，**幅度可设**（1-20 %/格，默认 2%）
  - **鼠标中键单击任务栏**即可切换静音（系统自带静音图标 OSD）
  - 幅度 = 2% 时走系统原生音量键（带 Win10/11 自带 OSD 浮窗），其它幅度走 COM 精确调整
  - 自动覆盖主显示器任务栏和副显示器任务栏（多屏友好）
  - 启用 / 幅度 / 中键 三个开关独立可关；未启用时所有拦截放行
- **开机自启**：勾选后自动复制程序到 `%LOCALAPPDATA%\KeyMapper` 并写入注册表，开机后台运行
- **系统托盘**：最小化自动缩到托盘；右键菜单可快速启用/停用映射、以管理员身份重启、退出
- **关闭行为**：点击窗口右上角"×"时，可选择"最小化到托盘"、"退出程序"或"取消"（可勾选"记住选择"）

## 使用方法

1. 双击 `KeyMapper.exe` 启动
2. 点【＋添加映射】→ 点击输入框后按下要改的键（如 PageUp）→ 选择动作类型：
   - "映射为按键/功能"：在列表中搜索/选择目标键（如"音量增大"）
   - "打开网页"：输入网址，如 `www.bing.com`
   - "启动程序"：点【浏览…】选择 exe 或快捷方式
   - "屏蔽此键"：无需其他设置
3. 确定后即可生效；点【按键测试】可实时查看按键高亮；点【注入自检】可验证映射是否生效
4. 需要临时恢复时，点【停用映射】；再次点击可恢复
5. 勾选【开机自动启动】后开机自动运行
6. **任务栏滚轮调音量**：勾选主界面右下角的【任务栏滚轮调音量】，把鼠标移到任务栏上滚动滚轮即可调音量，按中键切换静音；幅度控件可改每格调整量

## 编译方法

需要修改源码时，确保系统有 .NET Framework（Win7+ 自带），双击 `build.bat` 即可生成 `KeyMapper.exe`（自动嵌入键盘图标）。

## 注意事项

- 开机自启会把程序复制到 `%LOCALAPPDATA%\KeyMapper`，请勿手动删除该文件夹（取消勾选自启后再删）
- 程序使用系统级低权限键盘/鼠标钩子，**无需管理员权限**；若目标程序以管理员身份运行导致映射无效（UIPI 拦截），请用托盘菜单"以管理员身份重启"
- 首次运行可能弹出 SmartScreen 提示（未签名程序），点"更多信息 → 仍要运行"
- 个别杀毒软件可能对键盘钩子误报，添加信任即可
- 仅支持"单键→动作"映射，不支持组合键
- 锁屏状态下 Windows 会禁止输入注入，映射不生效属正常现象，解锁即可
- 任务栏滚轮调音量：把鼠标指针放到任务栏（开始按钮、托盘区、固定应用图标、空白处等都在内）再滚轮；自动隐藏任务栏缩走后自然不触发
- 卸载：退出程序 → 取消勾选"开机自动启动" → 删除程序文件夹

## 更新记录

- **v2.3**：新增"任务栏滚轮调音量"——主界面右下角开关，鼠标在任务栏上滚轮调音量（幅度可设 1-20%/格）、中键单击切换静音；自动支持多显示器任务栏；幅度 2% 时调用系统原生音量键（带 Win10/11 自带 OSD 浮窗），其它幅度走 COM 精确调整
- **v2.2**：新增"打开网页"和"启动程序"两种映射动作（含浏览选择、自动补全协议、长按不重复触发）；界面优化（目标类型下拉切换、添加/编辑对话框重排版）；兼容旧版本配置文件
- **v2.1**：新增编辑映射（按钮+双击列表）；新增关闭行为选择（最小化到托盘/退出/取消，可记住选择）；修复按钮布局（分两组显示，不再被裁剪）
- **v2.0**：修复 SendInput 结构体错误（映射不生效的根因）；修复按键测试无响应；支持映射全部键盘按键；新增注入自检、管理员模式、扫描码注入
